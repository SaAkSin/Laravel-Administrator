# 필드 타입 - Key

`key`는 Eloquent 모델의 기본 키 값을 표시하는 필드입니다. 기본 키는 데이터베이스에서 관리되므로 편집 필드로 사용해도 수정할 수 없습니다.

```php {2}
'id' => array(
    'type' => 'key',
    'title' => 'ID',
);
```

모델 기본 키 이름과 같은 필드는 타입을 생략해도 자동으로 `key`로 처리됩니다.

필터로 사용하면 특정 기본 키 값을 직접 입력해 항목을 빠르게 찾을 수 있습니다.
